% \NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{style}[2026/04/13 Custom style with Latin Modern]

\makeatletter

% Grundpakete
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc} % bei aktuellem LaTeX oft nicht mehr nötig, aber für pdflatex ok
\usepackage{lmodern}

\usepackage{amsmath,amssymb,mathtools,bm}
\usepackage{enumitem}
\@ifclassloaded{beamer}{}{\usepackage{fancyhdr}}
\@ifclassloaded{beamer}{}{\usepackage{geometry}}
\@ifclassloaded{beamer}{}{\usepackage{titlesec}}
\@ifclassloaded{beamer}{}{\usepackage{setspace}}
\usepackage{graphicx}
\usepackage{multicol}
\usepackage{ragged2e}
\usepackage{systeme}
\usepackage{stmaryrd}
\usepackage{pgfplots}
\usepackage{xcolor}
\usepackage{siunitx}
\usepackage{bigints}
\usepackage{tikz-cd}
\usepackage{tikz}
\usepackage{silence}
\usepackage[hang,flushmargin]{footmisc}

\usetikzlibrary{calc}
\pgfplotsset{compat=1.18}

% Titel-/Überschriftenfont:
% statt Futura/Minion jetzt mit Latin Modern Sans
\newcommand{\rawtitlefont}{\sffamily\bfseries}
\newcommand{\titlefont}[1]{{\rawtitlefont\textcolor[HTML]{222222}{#1}}}

\renewcommand{\familydefault}{\sfdefault}


\@ifclassloaded{beamer}{}{%
  \geometry{a4paper, margin=2cm}
}

\@ifclassloaded{beamer}{}{%
  \titlespacing{\section}{0pt}{15pt}{-\parskip}
  \titlespacing{\subsection}{0pt}{10pt}{-\parskip}
  \titlespacing{\subsubsection}{0pt}{5pt}{-\parskip}
}

\setlength{\lineskiplimit}{5pt}
\setlength{\lineskip}{5pt}

% Mathe-Abstände
\newcommand*\squeezespaces[1]{%
  \thickmuskip=\scalemuskip{\thickmuskip}{#1}%
  \medmuskip=\scalemuskip{\medmuskip}{#1}%
  \thinmuskip=\scalemuskip{\thinmuskip}{#1}%
  \nulldelimiterspace=#1\nulldelimiterspace
  \scriptspace=#1\scriptspace
}
\newcommand*\scalemuskip[2]{%
  \muexpr #1*\numexpr\dimexpr#2pt\relax\relax/65536\relax
}

\@ifclassloaded{beamer}{}{%
  \newcommand{\stdtitle}[3]{%
    \setstretch{1.6}%
    \titlefont{\LARGE #1}\\
    \titlefont{\LARGE #2}\\
    \titlefont{\Large #3}\\
    \vspace{10pt}%
  }

  \titleformat{\section}
    {\rawtitlefont\color[HTML]{222222}\Large}
    {\thesection}{1em}{}
  \titleformat{\subsection}
    {\rawtitlefont\color[HTML]{222222}\large}
    {\thesubsection}{1em}{}
  \titleformat{\subsubsection}
    {\rawtitlefont\color[HTML]{222222}\normalsize}
    {\thesubsubsection}{1em}{}
}

\setlist[enumerate]{%
  font=\sffamily\bfseries,
  leftmargin=2.1em,
  labelwidth=0em,
  align=right,
  labelsep=1.1em
}

\@ifclassloaded{beamer}{}{%
  \pagestyle{fancy}
  \fancyhead{}
  \fancyfoot{}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
}

% Mathe-Befehle
\newcommand\SET[2]{\{#1\mid#2\}}
\newcommand\Set[1]{\left\{#1 \right\}}

\newcommand\id{\text{id}}
\newcommand\Image{\text{Im}}
\newcommand\rank{\text{rank}}
\newcommand\Hom{\text{Hom}}
\newcommand\Span{\text{span}}

\newcommand\mol{\text{mol}}
\DeclareSIUnit\bar{bar}

\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\N}{\mathbb{N}}

\renewcommand{\d}{\delta}
\newcommand{\e}{\varepsilon}

\DeclarePairedDelimiter\ceil{\lceil}{\rceil}
\DeclarePairedDelimiter\floor{\lfloor}{\rfloor}
\DeclarePairedDelimiter\abs{\lvert}{\rvert}
\DeclarePairedDelimiter\norm{\lVert}{\rVert}

\newcommand\dul[1]{\underline{\underline{#1}}}

\newcommand{\fa}{\,\hbox{$\forall$}\,}
\newcommand{\ex}{\;\hbox{$\exists$}\;}
\newcommand{\nex}{\;\hbox{$\nexists$}\;}

\let\oldabs\abs
\def\abs{\@ifstar{\oldabs}{\oldabs*}}

\@ifclassloaded{beamer}{}{%
  \setstretch{1.3}
  \raggedright
}

\everymath{\displaystyle}

\makeatother